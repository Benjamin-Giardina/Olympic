package omlympic;

public class MedalCalculation {

    String fPlayer, sPlayer, country;
    double[] score1;
    double[] score2;

    /* public MedalCalculation(String fp, String sp, String c, double[] s1, double[] s2) {
        fPlayer = fp;
        sPlayer = sp;
        country = c;
        score1 = new double[s1.length];
        score2 = new double[s2.length];
        for (int x = 0; x < s1.length; x++) {
            score1[x] = s1[x];
            score2[x] = s1[x];
        }
    }*/

 /* public static int sequentialSearch(int[] array, int value) {
        int index, reference;
        boolean found;

        found = false;
        reference = -1;
        index = 0;

        while (!found && index < array.length) {
            if (array[index] == value) {
                reference = index;
                found = true;
            }
        }
        return reference;
    }*/
}
